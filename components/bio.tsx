import { Box } from '@chakra-ui/react'
import styled from '@emotion/styled'

export const WorkSection = styled(Box)`
  display: flex;
  padding-left: 1.5em;
  text-indent: auto;
`

export const WorkTimes = styled.span`
  font-weight: bold;
  margin-right: 1em;
`
